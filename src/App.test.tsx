import * as React from 'react';
import * as ReactDOM from 'react-dom';
import App from './App';

it('renders without crashing', () => {
  // let div = document.createElement('div');

  // let script = document.createElement('script');
  // script.setAttribute('type', 'text/javascript');
  // script.setAttribute('src', './services/services.js');

  // let script1 = document.createElement('script');
  // script1.setAttribute('type', 'text/javascript');
  // script1.setAttribute('src', './services/core.js');

  // let papaScript = document.createElement('script');
  // papaScript.setAttribute('type', 'text/javascript');
  // papaScript.setAttribute('src', 'https://cdnjs.cloudflare.com/ajax/libs/PapaParse/4.5.0/papaparse.js');

  // div.appendChild(script1);
  // div.appendChild(script);
  // document.body.appendChild(div);
  // ReactDOM.render(<App />, div);

  // ReactDOM.unmountComponentAtNode(div);
});
